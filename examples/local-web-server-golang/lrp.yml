proxy: 
  host: "localhost:9000"
source:
  host: "localhost:8080"
tasks:
  local-web-server:
    monitor:
      paths:
        - "./main.go"
      ignores:
        # vim
        - "*.swp"
        - "*~"
    commands:
      gin:
        execute: "go run main.go"
        needs_restart: true
